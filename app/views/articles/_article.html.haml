%article
  .header
    %header
      %h2= article.title
  .body
    = simple_format article.body
  .footer
    %footer
      %p.left
        - article.tags.each do |tagging|
          %span.tag= link_to tagging.name, tagged_articles_path(name: tagging.name)
      %p.right= l article.published_at
